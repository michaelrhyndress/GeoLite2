version: '3'
services:
  web:
      restart: always
      build:
          dockerfile: Dockerfile
      volumes: []
      environment:
          DEBUG: 'false'
          ENV: 'PRODUCTION'
  tests: {}
  server:
      restart: always
      build:
          dockerfile: Dockerfile
      environment:
          DEBUG: 'false'
          ENV: 'PRODUCTION'